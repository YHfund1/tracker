# 安全发布边界

## 默认禁止项

未被用户明确授权时，禁止：

- `git commit`
- `git push`
- 任何部署命令
- 修改远程仓库地址
- 写入/展示任何凭据

## 允许发布的前置条件

只有当用户明确说“现在发布”，并明确目标仓库/分支后，才进入发布步骤。

## 发布前检查

1. 再次确认本次提交文件范围。
2. 再次确认没有误带隐私信息。
3. 明确回报即将执行的发布命令。

## 共享包脱敏规则

共享技能包中不得出现：

- 用户名
- 邮箱
- 密码
- Token
- SSH 私钥/公钥
- 远程仓库专有地址
