# 校验与交付

## 必做校验

1. JSON 校验：全部改动 JSON 可解析。
2. 日期校验：页面日期、数据更新时间一致。
3. 内容校验：关键栏目都已更新，不保留明显过时文案。
4. 依赖校验：动态页面仍能拉到对应 JSON。

## 输出模板（给用户）

交付时必须说明：

- 已更新页面清单
- 已更新数据文件清单
- 未完成项或风险项
- 若用户未要求发布：明确“仅本地更新，未推送”

## 发布规则（默认关闭）

- 只有用户明确说“发布/推送/部署”时才进入发布步骤。
- 发布前先二次确认目标仓库与分支，避免误操作他人项目。
